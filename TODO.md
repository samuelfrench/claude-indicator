# Claude Indicator TODO

- [ ] **2026-09-07: Traffic Report link: combined full gate passed; independent review and live activation next.** Feature branch `codex/indicator-traffic-report-20260907` starts at fetched `master` / `origin/master` `520410abbc8907b1bf50332256fc71b305d32cf0`. `claude_widget.py:7968` adds the compact keyboard-accessible footer link; `_setup_tray_icon` places a matching action immediately after Smart TODOs; `_open_traffic_report` opens the fixed `http://127.0.0.1:5173/` with Qt's desktop URL handler. Long status messages use middle elision, preserving the complete text in the tooltip and accessibility description. Focused `PYTHONPATH=. QT_QPA_PLATFORM=offscreen /home/sam/miniconda3/bin/python3 -m pytest -q tests/test_widget_ui.py` passes **106 tests + 3 subtests**; one-off Qt interaction checks confirm mouse click, Tab then Space, and tray activation all open the exact URL, browser-open failure is visible, and normal/rate-limited/long-error status fits alongside the link in a 340px panel (195px status / 68px link). Evidence: `/tmp/indicator-traffic-report-20260907/implementation-qt-smoke.json` plus three `implementation-*.png` images; this inert offscreen smoke intercepts the browser handler and does not prove native activation. Combined checkpoint: merged concurrent disk-row release `c20019b` cleanly as `9200a72da97331ad61630279ce48c961d91d8949`, preserving both disk metrics and terminal recovery. Full `PYTHONPATH=. QT_QPA_PLATFORM=offscreen /home/sam/miniconda3/bin/python3 -m pytest -q` passes **406 tests + 10 subtests** (5.32s), and compile/diff checks pass. The earlier pre-merge full pass was 397 + 10; the combined result supersedes it. Remaining: independent review, integrate/push, then restart `app-claude\x2dwidget@autostart.service` and click the actual footer link to verify the Traffic Report dashboard. Destination checks verified both `/` and `/api/report` HTTP 200; native pre-change window is 340x861 and Firefox is the default browser. Done means the live Indicator exposes the link and it opens that dashboard. No new service, provider request, billing, or approval clock; the existing local report server remains independently managed.

- [x] Add per-disk (HDD/SSD) rows under the expanded SYSTEM section (Sam, 2026-09-07). `DiskMetrics` + `SystemMetricsReader._read_disks` in `claude_widget.py` discover physical unhidden `/sys/block` devices (loop/zram/dm skipped), compute busy % from `/proc/diskstats` `io_ticks`, R/W byte rates from sector deltas, and df-style fill summed over each disk's mounted partitions via `statvfs`; `SystemMetricsRow` adds one 22px bar row per disk between GPU and NET (`nvme0n1  R2.0M/s W29M/s · 94%`), widens the label/detail columns from font metrics, and lists HDD/SSD, model, size, fill and mount points in the tooltip. Host shows `nvme0n1` (root, 94% full), `nvme1n1` (unmounted, `—`), `sda` (HDD, 57%). Expanded SYSTEM now grows the 340x861 panel to 340x1015 (fits the 1440px screen; not part of the 824px tall-sections budget because SYSTEM expansion is not in that test). Tests: 8 reader tests in `tests/test_system_metrics.py`, 1 UI test in `tests/test_widget_ui.py`; full suite `406 passed, 10 subtests passed`. Deployed via `systemctl --user restart 'app-claude\x2dwidget@autostart.service'`, visually verified expanded on PID `975210`. Gotcha: `f"{12.5:.0f}"` is `12` (banker's rounding) — don't assert `13%` in tests. Not done: SMART temperature/health (needs root or `smartctl` polling), per-partition breakdown, and per-disk collapsed-row summary (collapsed row stays CPU/RAM/GPU/net so the 340px width still fits).
- [x] Add an OpenCode Go usage row (committed `d0c5d3cbf8dad8b71c78c3fe7ef060be9a9a9b28`, deployed 2026-09-07): `OpencodeGoUsageRow` shows the dollar-metered `5H/$12 · 7D/$30 · 30D/$60` utilization percents from `GET https://opencode.ai/zen/go/v1/usage` (Bearer `opencode-go` key; env `OPENCODE_GO_API_KEY` first, else owner-only mode-0600 `~/.local/share/opencode/auth.json`), with per-window dollars used, reset times, and a `!` marker for `rate-limited` windows. Sam approved growing the panel past the 800px budget; the tall-sections test budget is now 824 and `clamp_to_available_screen` pins the top when the window is taller than the screen. Live endpoint verified: rolling 86%, weekly 34%, monthly 17%. Full suite `380 passed, 10 subtests passed`. Deployed by stopping the stale worktree unit `claude-widget-terminal-recovery.service` (was still running `.worktrees/indicator-terminal-recovery/claude_widget.py` — the reason the row was missing) and pointing `~/.config/autostart/claude-widget.desktop` back at the main repo; `app-claude\x2dwidget@autostart.service` is healthy as PID `850423` on DISPLAY=:1, window 340x861. Screenshot `/tmp/claude-indicator-go-row.png` visually confirmed by Sam 2026-09-07.
- [x] Read task-loop status from local configuration only; no longer scan DynamoDB every 60 seconds.
- [x] Document the controlled `mergepdfnow.com` registrar contact-recovery exception and hash-verified immutable-field rule without storing an address or other PII.
- [x] Complete and review the immediate Cloudflare cutover for all 16 active Route 53 zones while leaving every source zone intact.
- [ ] Delete the 16 Route 53 source zones only after `2026-07-20T15:18:57.201000Z`, each rolling stale-cache clock, a 13-round/one-hour resolver clean window, and both full delayed gates pass.
- [x] Show the Fable-specific usage limit: parse the `/api/oauth/usage` `limits` array (model-scoped `weekly_scoped` entries) and render one `UsageBar` per model cap — currently `Fable (7-Day)` — with legacy `seven_day_opus`/`seven_day_sonnet` fallback and `last_usage.json` round-trip.
- [x] Keep minimize recoverable: collapse the header button to a visible right-edge restore sliver while retaining shared tray hide/show behavior.
- [x] Show Codex usage-limit percentage: `CodexUsageRow` reads the live base `codex` bucket through local `codex app-server` `account/rateLimits/read`, renders only present windows, preserves local token/thread totals, and uses cached session `token_count` events only as a fail-safe.
- [x] Keep startup visible and avoid hidden tray-only behavior: force initial sizing/raise and only hide-on-close when tray is available.
- [x] Handle the current Codex rate-limit schema where the base bucket can expose one 7-day primary window and `secondary: null`, while retaining support for two-window responses.
- [x] Add the Smart TODO tray command center with global inbox capture, cross-project urgency ranking, safe source navigation, and local-only persistence. Shipped to `master`, verified against the real 34-file TODO corpus, and restarted in production via `claude-widget-codex.service`.
- [x] Add persistent Smart TODO dismiss state: `Dismiss` preserves every TODO source, hides finished tasks from active views and counts, and exposes them in `Finished` via local-only `~/.claude/smart_todos_finished.json` state.
- [x] Ship the full Smart TODO daily workflow release: seven-item Today docket with pins, snooze/wake, Restore, new/changed and duplicate review, project drill-down, Copy Context, conservative stale views, local-only atomic workflow state, and lock-screen-safe tray recovery.
- [x] Collapse the standalone Ollama indicator into a compact expandable Local AI section in this panel, reusing local GPU and task-loop data and adding Ollama/ComfyUI REST status without DynamoDB polling.
- [x] Add a DeepSeek row with rolling 24-hour OpenCode-recorded API spend, official current account credit, secure credential fallback, and conservative atomic balance-snapshot estimates.
- [x] Add a MiniMax row with live coding-plan 5-hour/weekly quota utilization from `GET /v1/token_plan/remains`, 24-hour local OpenCode token volume, message count, and latest model, using the same owner-only credential fallback as DeepSeek.
- [x] Add an ollama scheduler watchdog: a 5-minute systemd timer that restarts `ollama` once its scheduler has been wedged for 15 minutes, detected via stale keep-alive expiry or a hung no-token load probe.
- [x] Add a local-model token tracker: today and all-time token volume plus message counts from the OpenCode ollama-provider ledger, labelled in the UI so the source scope is explicit.
- [x] Break the OpenCode ledger figures down per model so MiniMax, DeepSeek and ollama traffic are individually attributable, each line tagged with its provider.
- [x] Add the terminal tabs workflow surface: a `TABS` row summarising live claude/codex/opencode terminal sessions with a NEEDS-YOU count, plus an animated side panel docked to the widget with per-session cards — WORKING/WAITING/NEEDS YOU/PARKED grouping, park/unpark, persistent notes, and click-to-jump terminal navigation via xdotool.
- [x] Improve the terminal tab selector (shipped and independently reviewed 2026-09-04): final code `5a0ebe34021677b76365ef7d37524cecc36f27b4` provides the compact 320px/62px-card panel at `claude_widget.py:5418`, exact GNOME TTY selection at `claude_widget.py:1933` with one fstat-validated held pts descriptor from `claude_widget.py:1785`, fail-closed final foreground/index commit at `claude_widget.py:2224`, linked selector/main drag at `claude_widget.py:7669`, and selector-visible minimize at `claude_widget.py:7811`. It preserves a clean custom title, uses the sanitized fallback only for ambiguous cleanup, retains external third-index changes, and restores selector-owned failures. Focused `PYTHONPATH=. QT_QPA_PLATFORM=offscreen /home/sam/miniconda3/bin/python3 -m pytest -q tests/test_terminal_sessions.py tests/test_widget_ui.py` passes `147 passed, 5 subtests passed`; full `PYTHONPATH=. QT_QPA_PLATFORM=offscreen /home/sam/miniconda3/bin/python3 -m pytest -q` passes `356 passed, 5 subtests passed`; py_compile/diff-check and final review pass. The pushed SHA is deployed through `app-claude\x2dwidget@autostart.service` as healthy xcb PID `4043169` (started `2026-09-04 12:08:33 CDT`); its mapped main window is 340x827. A native xcb/xdotool proof using the current 8-session snapshot rendered the compact panel at 320x582, moved main and selector exactly +45,+30 from a real selector-header pointer drag, produced minimize map states main=`IsUnMapped`/selector=`IsViewable`/28x72 sliver=`IsViewable`, and restore states main=`IsViewable`/selector=`IsViewable`/sliver=`IsUnMapped`; the screenshot was visually inspected. A real unlocked desktop proof then selected duplicate Coffee sessions `pts/0 -> tab 0` and `pts/4 -> tab 1` in GNOME window `46137354`; both calls returned true with `active_window=46137354` and marker-clean titles, after which original indices for windows `46137354` and `47680729` and the pre-proof active window `102760462` were restored. The alternative second-session GNOME proof was rejected after 3/3 isolated attempts hit the host portal's shared-runtime mount conflict; none of those attempts count as evidence and all ephemeral processes/scripts were removed. No billable service was added or changed.
- [x] Fix four validated network-indicator review defects before deployment (found at `bed7918`, checkpointed at `ff26ed0`, offline-complete 2026-09-04). `_prev_network` at `claude_widget.py:712-714` now retains each selected interface's counters; `_read_network` at `claude_widget.py:738-805` rebaselines to zero when any per-interface RX or TX decreases at `claude_widget.py:790-795`, while summing normal monotonic deltas. The deterministic regression at `tests/test_system_metrics.py:172-193` uses equal-metric `eth0/eth1=(1000,1000)` followed 2 seconds later by reset `eth0=(100,100)` and growing `eth1=(2900,2900)`: the old aggregate code emitted `500 B/s`, the fix emits zero, and the next sample computes `(200,400) B/s`. `SystemMetrics.net_error` at `claude_widget.py:436` distinguishes route reads, counter reads/missing counters, and clock failures; route-read/reset coverage is at `tests/test_system_metrics.py:195-233`, and tooltip coverage at `tests/test_widget_ui.py:2038-2068` proves `/proc/net/route` failure is not mislabeled as no route and missing selected counters name `/proc/net/dev`. `SystemMetricsRow` gains its accessible name/focus at `claude_widget.py:6722-6723` and shared mouse/Space/Enter toggle at `claude_widget.py:6849-6876`, covered at `tests/test_widget_ui.py:2070-2096`. Fractional promotion/capping is at `claude_widget.py:668-696`, with `1023.5 B/s -> 1.0 KiB/s` and `999.5 TiB/s -> 999+ TiB/s` covered at `tests/test_system_metrics.py:263-275`. Pre-fix `tests/test_system_metrics.py` produced `4 failed, 8 passed, 5 subtests passed`; after the fix it passes `12 passed, 5 subtests passed`. Final-tree focused `PYTHONPATH=. QT_QPA_PLATFORM=offscreen /home/sam/miniconda3/bin/python3 -m pytest -q tests/test_system_metrics.py tests/test_widget_ui.py` passes `110 passed, 8 subtests passed` in `1.73s`; final-tree full `PYTHONPATH=. QT_QPA_PLATFORM=offscreen /home/sam/miniconda3/bin/python3 -m pytest -q` passes `372 passed, 10 subtests passed` in `6.47s`; py_compile of the three changed Python files and `git diff --check` pass. Reviewer otherwise approved route selection, width recovery, and reset/unavailable layout. No production restart or traffic generation occurred.
- [x] Deploy and visually verify the compact IPv4 default-route network activity indicator (completed 2026-09-04). Final code `b5d9ea1b5da288b9de585e25abfe85780ce27a8f` was independently rereviewed with no findings; the root final audit reran focused `PYTHONPATH=. QT_QPA_PLATFORM=offscreen /home/sam/miniconda3/bin/python3 -m pytest -q tests/test_system_metrics.py tests/test_widget_ui.py` at `110 passed, 8 subtests passed` in `1.61s`, full `PYTHONPATH=. QT_QPA_PLATFORM=offscreen /home/sam/miniconda3/bin/python3 -m pytest -q` at `372 passed, 10 subtests passed` in `12.66s`, plus clean pycompile and diff-check. `SystemMetrics` at `claude_widget.py:423`, route/dev parsers and formatter at `claude_widget.py:618`, `claude_widget.py:647`, and `claude_widget.py:668`, `_read_network` at `claude_widget.py:738`, and `SystemMetricsRow` at `claude_widget.py:6710` read procfs directly, select every UP non-reject lowest-metric IPv4 default route, calculate monotonic per-interface-safe aggregate receive/transmit rates, expose honest source errors, and update on the unchanged 3-second timer at `claude_widget.py:81` and `claude_widget.py:7455`. Twelve reader/parser/formatter tests run through `tests/test_system_metrics.py:54-280`; four UI tests run through `tests/test_widget_ui.py:1943-2096`. The network delta did not modify terminal selector or terminal-focus code. The user unit `app-claude\x2dwidget@autostart.service` was restarted exactly once and is healthy as xcb PID `154974`, started `2026-09-04 12:56:05 CDT`, running `/home/sam/miniconda3/bin/python3 /home/sam/claude-workspace/claude-indicator/claude_widget.py` with `libqxcb` mapped and no warning-or-higher journal entries since start. The native production main window was `IsViewable` at `x=3080,y=40` on the 3440x1440 primary screen. `/tmp/claude-indicator-b5d9ea1-collapsed.png` captured the 340x827 collapsed widget visibly showing CPU/RAM/GPU plus `↓85K/s` and `↑381K/s` without overlap or clipping; after the proof, `/tmp/claude-indicator-b5d9ea1-collapsed-final.png` captured the restored 340x827 state with `↓123K/s` and `↑65K/s`. A native pointer click expanded System to a fully visible 340x915 window; `/tmp/claude-indicator-b5d9ea1-expanded.png` visibly shows CPU/RAM/GPU including `46°C` and the NET row at `↓164 KiB/s` and `↑28 KiB/s`, fully inside the screen without clipping. The row was collapsed back to 340x827, and pre-proof active window `37748759` plus pointer `(820,177)` were restored. A separate read-only natural-traffic reader proof sampled `enp12s0` available at the required `0/0` first-sample baseline and, 3.2 seconds later, measured RX `1124005.102 B/s` (`1.1 MiB/s`) and TX `80844.436 B/s` (`79 KiB/s`) with blank `net_error`. No traffic was generated; no billable service or external approval clock was added.
- [x] Restore drag-to-move after the host rebooted into a GNOME Wayland session: prefer XWayland (`QT_QPA_PLATFORM=xcb;wayland`) so client-side positioning, always-on-top and xdotool work again, with a `startSystemMove()` drag fallback for pure Wayland.
- [ ] Consider a tray notification (rate-limited) when a session flips to NEEDS YOU, and journald-based local token tracking for non-OpenCode clients (see memory: local_token_sources).





- [x] Terminal recovery shipped 2026-09-07: deployed application code `89f3dabad1add991f05a960307b3e44449dee1c1` on `master` includes recovery plus the concurrent OpenCode Go release. `terminal_recovery.py:53` scans every owned controlling terminal and `terminal_recovery.py:130` commits boot-scoped directory/program inventories to private `~/.local/state/claude-indicator/terminals.sqlite3`; `terminal_recovery_ui.py:37` provides Live / Last boot snapshot / All saved, search and copy details; `claude_widget.py:8172` records on the existing 5-second timer with visible errors and last-good preservation. Full `PYTHONPATH=. QT_QPA_PLATFORM=offscreen /home/sam/miniconda3/bin/python3 -m pytest -q` passes `397 passed, 10 subtests passed`; source Python is unchanged from tested combined `d0eff41` (later merges were documentation only). Independent review approved scoped fixes; tests cover actual writer SIGKILL during uncommitted writes with intact prior inventory, new/empty boots, recognizable foreground programs, permission failures, and tray hide. Native live/search and fixture previous-boot views were inspected at 960x620 and 780x580 under `/tmp/indicator-recovery-proof.AvpB3l/`. Final production `final-panel.png` and `final-recovery.png` visibly prove the TABS footer and recovery window on xcb PID `864473`, started `2026-09-07 09:41:56 CDT` through healthy `app-claude\x2dwidget@autostart.service`; autostart and ExecStart both use the normal primary checkout. The final readback at capture 52 had 10 live terminals exactly matching a fresh proc scan, 13 retained records, save age 4.5 seconds, database integrity ok, and mode 0600 / directory 0700. Both owned proof windows were closed; Bash PID `767784` and sleep PID `767811` remain archived with first observation `2026-09-07 09:38:03 CDT` (before the final widget restart) and non-null closed timestamps. This proves same-boot restart retention and exited-terminal retention; prior-boot recovery was verified with a separate fixture and abrupt-writer tests, without rebooting the host. Records begin with this release, capture program names/directories (no arguments/environment/scrollback), and do not restart commands. No billable services or approval clocks added.
